{% extends 'base.html.twig' %}

{% block  body %}
    <div class="url-detail" data-url-detail="{{ path('app_delivery_detail', {'slug': 0}) }}"></div>
    <div class="customer-todo">
    </div>
    <hr>
    <div class="customer-done">
    </div>

            <th>Delivery Company</th>
            <th>Receiver</th>
            <th>Address</th>
    <p>Deze moet meer informatie krijgen voor specifiek bezogers</p>
{% endblock %}